define(['Magento_Ui/js/lib/validation/utils'],function(utils){'use strict';return function(price,input){var result,lastInputSymbol=input.slice(-1),inputPercent=input.slice(0,-1),parsedPercent=utils.parseNumber(inputPercent),parsedPrice=utils.parseNumber(price);if(lastInputSymbol!=='%'){result=input;}else if(input==='%'||isNaN(parsedPrice)||parsedPercent!=inputPercent||isNaN(parsedPercent)||input===''){result='';}else if(parsedPercent>100){result='0.00';}else if(lastInputSymbol==='%'){result=parsedPrice-parsedPrice*(inputPercent / 100);result=result.toFixed(2);}else{result=input;}
return result;};});